name: Verify pull request to main

on:
  pull_request:
    branches:
      - "main"

permissions:
  contents: read

jobs:
  test:
    name: Run Tests
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v7

      - name: Run tests and verify code coverage level
        run: make coverage

  lint:
    name: Lint
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install lint tools
        run: make install-lint-tools

      - name: Lint
        run: make lint

  build:
    name: Test build process
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v7

      - name: Build
        run: make build
